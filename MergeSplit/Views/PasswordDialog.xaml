<Window x:Class="MergeSplit.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MergeSplit.Views"
        mc:Ignorable="d"
        Title="PasswordDialog" Height="250" Width="800">

    <Grid Margin="0,0,0,265">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Enter your password to proceed further"
               Grid.Row="0" Margin="10"/>

        <TextBox x:Name="tbPassword" Grid.Row="1"
                 Margin="10" VerticalAlignment="Center"/>

        <StackPanel Orientation="Horizontal" Grid.Row="2"
                    HorizontalAlignment="Right" Margin="10">
            <Button x:Name="btnOK" Content="OK"
                    Width="75" Margin="5"
                    IsDefault="True" />
            <Button x:Name="btnCancel" Content="Cancel"
                    Width="75" Margin="5"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
